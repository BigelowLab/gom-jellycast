---
title: "Jellyfish Sighting Forecasts"
format:
  html:
    code-fold: true
---

```{r setup, include=FALSE}
source("setup.R")
knitr::opts_chunk$set(echo = TRUE)
```

# Lion's Mane Jellyfish Sighting Forecast
```{r lionsmane, echo=FALSE}
img = update_forecast(version = "v0", v = "v0.015", model = "rf")
grid.raster(img)
```

# Moon Jellyfish Forecast
```{r moon, echo=FALSE}
img = update_forecast(version = "v0", v = "v0.021", model = "rf")
grid.raster(img)
```

# White Cross Jellyfish Forecast
```{r whitecross, echo=FALSE}
img = update_forecast(version = "v0", v = "v0.028", model = "rf")
grid.raster(img)
```

# Comb Jellyfish Forecast
```{r comb, echo=FALSE}
img = update_forecast(version = "v0", v = "v0.033", model = "rf")
grid.raster(img)
```